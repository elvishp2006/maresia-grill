services:
  - type: web
    name: maresia-grill-production
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /s/*
        destination: /index.html
      - type: rewrite
        source: /*
        destination: /index.html
